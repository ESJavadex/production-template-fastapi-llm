# Production-Ready FastAPI + LLM Dependencies
# ================================================

# === Core Framework ===
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0

# === LLM & AI ===
openai==1.54.3
tenacity==9.0.0  # Retry logic

# === Observability ===
# Choose one or install all for flexibility
langfuse==2.52.2  # LLM observability
# langsmith==0.1.140  # Alternative: LangChain observability
# logfire==0.59.0  # Alternative: Pydantic observability

# === Caching & Storage ===
redis[hiredis]==5.2.0  # Redis client with C parser

# === Security ===
cryptography==43.0.3  # Encryption for GDPR compliance
python-jose[cryptography]==3.3.0  # JWT tokens (if using auth)

# === HTTP & Networking ===
httpx==0.27.2  # Async HTTP client
aiofiles==24.1.0  # Async file operations

# === Testing ===
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.27.2  # For testing FastAPI
faker==30.8.2  # Generate fake data for tests

# === Load Testing ===
locust==2.32.2  # Load testing tool

# === Code Quality ===
black==24.10.0  # Code formatting
ruff==0.7.3  # Fast linting
mypy==1.13.0  # Type checking
bandit==1.7.10  # Security linting

# === Monitoring & Metrics ===
prometheus-client==0.21.0  # Prometheus metrics
sentry-sdk[fastapi]==2.17.0  # Error tracking

# === Deployment ===
gunicorn==23.0.0  # Production WSGI server
